# Frontend

This Next.js app is the frontend shell for the Eccom backend.

## Run locally

```bash
cd frontend
npm install
npm run dev
```

By default it talks to:

```text
http://localhost:5000
```

You can override that with:

```text
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
```
